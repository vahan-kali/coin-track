import React from "react";
import styled from "styled-components";

const FavoriteCoins = () => {
  return <Wrapper>Favorites</Wrapper>;
};

const Wrapper = styled.div`
  padding: 10px;
`;

export default FavoriteCoins;
